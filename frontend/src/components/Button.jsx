import React from 'react'

const Button = ({placeholder}) => {
  return (
    <div>
      <button className='btn px-5 text-white' id='btnn'>{placeholder}</button>
    </div>
  )
}

export default Button
