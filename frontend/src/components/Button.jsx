import React from 'react'

const Button = ({placeholder}) => {
  return (
    <div>
      <button className='btn px-5  bg-white fs-5' id='btnn'>{placeholder}</button>
    </div>
  )
}

export default Button
